import next from 'eslint-config-next'
import boundaries from 'eslint-plugin-boundaries'
import jsxA11y from 'eslint-plugin-jsx-a11y'

// Flat-config для ESLint 9 / Next 16 (eslint-config-next — готовый flat-массив).
// Экспериментальные react-compiler-правила Next 16 приглушены до warn: они агрессивно
// флагают валидные паттерны (guard-эффекты, запись cookie); чистка под них — отдельная задача.
// Запреты синтаксиса живут КОНСТАНТОЙ, потому что flat-config заменяет запись
// правила целиком: блок ниже, добавляющий узду карточки для кода вне shared/ui,
// обязан перечислить и все остальные — иначе фичи молча теряют i18n-барьер и
// прочее (поймано `eslint --print-config` при разведении правил по областям).
const RESTRICTED = [
        {
          selector: 'ConditionalExpression:matches([consequent.value=/[а-яА-ЯёЁ]/], [alternate.value=/[а-яА-ЯёЁ]/])',
          message:
            'Двуязычную строку из тернарника вынеси в словарь i18n (t()/tr() из @/shared/i18n): третий язык не должен требовать правки 100+ файлов. Технический литерал (не UI) — оставь // eslint-disable-next-line no-restricted-syntax.',
        },
        // Ф3 трека i18n-extraction: локальные say-хелперы и tr с инлайновыми
        // литералами были легальным обходом тернарного правила — после экстракции
        // (fe#646+) оба запрещены: строка живёт в dict/en.ts + dict/ru.ts.
        {
          selector: "VariableDeclarator[id.name='say']",
          message:
            'Локальный say(en, ru) запрещён (трек i18n-extraction, Ф3): добавь ключ в shared/i18n/dict/en.ts + ru.ts и используй t(key, lang). Плейсхолдеры — {n}/{a}/{b} + .replace().',
        },
        {
          // Property-селектор, а не properties.0: литерал ловится в ЛЮБОЙ позиции
          // объекта — tr({ en: value, ru: 'литерал' }) тоже запрещён (Codex #650).
          selector: "CallExpression[callee.name='tr'] > ObjectExpression > Property:matches([value.type='Literal'], [value.type='TemplateLiteral'])",
          message:
            'tr({ en: …, ru: … }) с инлайновыми литералами запрещён (трек i18n-extraction, Ф3): строка должна жить в словаре dict/. tr() — только для LocaleText-КОНТЕНТА из данных.',
        },
        // «У нас shadcn строго» (владелец, 2026-07-21, повторно): браузерные
        // формоэлементы запрещены — только shared/ui (Radix). Radix сам рендерит
        // скрытый нативный select для форм — правило ловит лишь НАШ JSX.
        {
          selector: "JSXOpeningElement[name.name='select']",
          message: 'Браузерный <select> запрещён — используй Select из @/shared/ui/select (shadcn/Radix, name для форм поддерживается).',
        },
        // Узда Ф7: кегли — только лестница ролей (11/12/12.5/13/14/16/18, control.ts
        // TEXT) и герои ≥20px (включая дробные и трёхзначные). Полупиксельный
        // зоопарк (Ф5) не должен вернуться. Легитимное исключение (герой-пара к
        // SearchField lg) — точечный disable. ⚠️ Счётные суппрессии общие по
        // файлу на rule id: в файле с i18n-baseline обмен «тернарник → кегль»
        // может проскочить; кнопочная узда поэтому живёт ОТДЕЛЬНЫМ правилом
        // react/button-has-type ниже (находка Codex по #622).
        // С Ф6 все размеры в rem (px→rem кодмод, масштаб через корневой
        // font-size): текстовый кегль в px запрещён целиком, а из rem запрещены
        // конверсии старого зоопарка (0.5625=9px … 1.1875=19px, кроме ступеней
        // лестницы). Герои ≥1.25rem (20px+) свободны.
        {
          selector: 'Literal[value=/text-\\u005B/]',
          message: 'Произвольный кегль запрещён целиком: лестница живёт в теме Tailwind (`@theme` в globals.css) настоящими утилитами — text-caption / text-caption-lg / text-body-sm / text-body / text-body-lg / text-title / text-page / text-heading / text-stat, витрина — text-display(-lg), логотип — text-logo(-lg,-xl). Нужен новый кегль — добавь СТУПЕНЬ в тему (там же видно, что она появилась), а не число в разметку. Относительный размер (em от текста вокруг) — точечный disable с причиной.',
        },
        {
          selector: 'TemplateElement[value.cooked=/text-\\u005B/]',
          message: 'Произвольный кегль запрещён целиком: лестница живёт в теме Tailwind (`@theme` в globals.css) настоящими утилитами — text-caption / text-caption-lg / text-body-sm / text-body / text-body-lg / text-title / text-page / text-heading / text-stat, витрина — text-display(-lg), логотип — text-logo(-lg,-xl). Нужен новый кегль — добавь СТУПЕНЬ в тему (там же видно, что она появилась), а не число в разметку. Относительный размер (em от текста вокруг) — точечный disable с причиной.',
        },
        // ── Кириллица в ИМЕНАХ (решение владельца 26.08.2026) ─────────────────
        // «Кириллице не место в именах — ни в коде, ни в тестах, только в документации».
        // Комментарии, док-блоки, строки интерфейса и имена тестов (у нас это СТРОКИ,
        // а не идентификаторы) правилом не задеваются — оно смотрит только Identifier.
        //
        // ⚠️ Записано через `\p{Script=Cyrillic}` с флагом `u`, а НЕ через диапазон
        // букв: класс символов в этих селекторах невозможен (см. предупреждение у узды
        // ширины ниже — из-за него она три недели не срабатывала вовсе).
        {
          // `:not(Property > .key)` — ключ объекта это ДАННЫЕ, а не имя: в таблице
          // транслитерации ключами стоят сами кириллические буквы (`{ а: 'a', б: 'b' }`),
          // и запрещать их бессмысленно — это и есть предмет модуля.
          selector: 'Identifier[name=/\\p{Script=Cyrillic}/u]:not(Property > .key)',
          message:
            'Кириллица в ИМЕНИ (переменная, функция, тип). Имя переводится ПО СМЫСЛУ, а не транслитерацией: `след` → `trace`, `немощь` → `giveUp`. Комментарии, строки интерфейса и имена тестов — по-прежнему по-русски.',
        },
        // Узда единой ширины (решение владельца 02.08.2026): рамка страницы —
        // центрированный блок с максимальной шириной И собственными полями. Их
        // было девять разных (1180…720) плюс четыре внутри админки, контент
        // прыгал при каждом переходе. Ловим сочетание mx-auto + w-full +
        // max-w-* + px/py; центрированные блоки ВНУТРИ страницы (карточка,
        // колонка формы) под него не попадают — у них нет всех четырёх.
        //
        // ⚠️ 26.08.2026: правило НЕ РАБОТАЛО НИ РАЗУ. Оно было написано через
        // `\u005B\s\S\u005D*`, а `\u005B` в регулярке — это ЛИТЕРАЛЬНАЯ скобка, а не
        // начало класса символов: шаблон требовал в строке текст `[\s\S]`, которого
        // там быть не может. Ловушка описана в комментарии к узде карточки ниже —
        // и в соседнем правиле осталась незамеченной. Проверено пробником: до
        // починки не срабатывало ни на `max-w-[73.75rem]`, ни на `max-w-page`.
        //
        // ⚠️ И вторая мера предосторожности, тоже оплаченная: сначала я починил
        // регулярку через `(?:.|\s)*`, оставив правило на ВСЕХ литералах подряд.
        // Линт перестал завершаться вовсе — альтернатива под звёздочкой даёт
        // катастрофический откат, а литералов в проекте тысячи, включая абзацы
        // словаря. Поэтому: правило ограничено className (как узда карточки) и
        // записано линейно — `.*` с флагом `s`.
        {
          selector:
            "JSXAttribute[name.name='className'] :matches(Literal[value=/(?=.*mx-auto)(?=.*w-full)(?=.*max-w-)(?=.*\\s(?:px|py)-)/s], TemplateElement[value.cooked=/(?=.*mx-auto)(?=.*w-full)(?=.*max-w-)(?=.*\\s(?:px|py)-)/s])",
          message: 'Своя ширина страницы запрещена — рамка одна на весь сайт: PAGE (или PAGE_X без вертикальных полей) из @/shared/ui/control. Читаемая колонка внутри страницы — PAGE_COLUMN. Правило ловит СОЧЕТАНИЕ mx-auto + w-full + max-w-* + отступ; с 26.08.2026 и по ступени темы (max-w-page) тоже — до этого хватало написать роль вместо числа, и рамка снова расползалась.',
        },
        // ── Узда высоты контролов (замер 13.08.2026) ───────────────────────────
        // Шкала жила в control.ts, но её обходили ДВУМЯ способами, и оба дали по
        // ряду разной высоты: 26 мест брали примитив и тут же перебивали ему
        // высоту классом (`<Button className="h-10">`), ещё 29 рисовали кнопку
        // руками из `h-8 rounded-md border`. Вторые не получали и тач-цель 44px
        // (TOUCH_MIN_H живёт в buttonClass) — на телефоне «Получить» была 32px
        // рядом с 44px-веткой, что владелец и увидел. Оба способа теперь запрещены.
        //
        // Роли выше md нет? Её надо ДОБАВИТЬ в шкалу (так появилась lg), а не
        // обойти классом: иначе одна и та же роль снова расползётся по числам.
        {
          selector:
            "JSXOpeningElement[name.name=/^(Button|IconButton|Input|Textarea|SearchField|SelectTrigger|SubmitButton|FloatingInput)$/] JSXAttribute[name.name='className'] :matches(Literal[value=/(^|\\s|:)(min-)?h-\\d/], TemplateElement[value.cooked=/(^|\\s|:)(min-)?h-\\d/])",
          message:
            'Высота примитива задаётся ТОЛЬКО пропом size (xs/sm/md/lg из shared/ui/control.ts), а не классом h-*. Нужна другая высота — добавь ступень в шкалу, иначе роль расползётся по числам.',
        },
        {
          // ⚠️ Классов символов тут быть НЕ МОЖЕТ: `[` ломает разбор селектора, а
          // приём `[` из правил выше означает ЛИТЕРАЛЬНУЮ скобку (он для того
          // и заведён — ловить `text-[13px]`), а не «любой символ». Первая версия
          // этого правила была написана с `[\s\S]*` и молча не срабатывала
          // вовсе. Поэтому «что угодно между» — через `(?:.|\s)*`.
          selector:
            "JSXOpeningElement[name.name=/^(button|a)$/] JSXAttribute[name.name='className'] :matches(Literal[value=/(^|\\s)h-\\d(?:.|\\s)*rounded-md|rounded-md(?:.|\\s)*\\sh-\\d/], TemplateElement[value.cooked=/(^|\\s)h-\\d(?:.|\\s)*rounded-md|rounded-md(?:.|\\s)*\\sh-\\d/])",
          message:
            'Рукописная кнопка (h-* + rounded-md) не получает ни шкалы, ни тач-цели 44px. Возьми Button/IconButton или buttonClass() из @/shared/ui/button-style — вид, высота и фокус придут оттуда.',
        },
        // ── Контрол БЕЗ высоты (её считает padding) ────────────────────────────
        // Дыра, через которую прошли все жалобы владельца 13.08.2026: узда выше
        // требует h-* вместе с rounded-md, а самые кривые места высоты НЕ ИМЕЮТ
        // ВОВСЕ. Кнопка «Отправить тест» в SMTP — `px-3.5 py-2` без высоты: 8+8
        // отступы + строка + рамки = 38px рядом с полем 32px. Замер: 71 такое
        // место в 47 файлах, и ни одно из них счётчик не видел.
        //
        // Высота контрола приходит ИЗ ШКАЛЫ, а не из арифметики отступов: иначе
        // соседи в ряду совпадают только случайно.
        {
          selector:
            "JSXOpeningElement[name.name=/^(button|input|a|select|textarea|Link)$/] JSXAttribute[name.name='className'] :matches(Literal[value=/rounded-(?:.|\\s)*(?:^|\\s)py?-(?:0\\.5|[1-9])|(?:^|\\s)py?-(?:0\\.5|[1-9])(?:.|\\s)*rounded-/], TemplateElement[value.cooked=/rounded-(?:.|\\s)*(?:^|\\s)py?-(?:0\\.5|[1-9])|(?:^|\\s)py?-(?:0\\.5|[1-9])(?:.|\\s)*rounded-/])",
          message:
            'Высоту контрола задаёт padding — значит она совпадёт с соседями только случайно. Возьми Button/IconButton/Input (или buttonClass) из shared/ui: высота придёт из шкалы control.ts. Не контрол (строка меню, ячейка, обёртка) — точечный disable с причиной.',
        },

]

// Узда карточки: внутри shared/ui рамка+фон+отступ — это работа примитива
// (SettingsSection, PageAside, dropdown-menu, DataTableV2…), запрещать нечего.
const CARD_RULE = // ── Узда карточки (Ф13, замер 13.08.2026) ─────────────────────────────
        // Примитива под роль «рамка + фон + отступ» не было вовсе, поэтому её
        // писали руками: 35 рецептов в 94 местах, семь внутренних отступов на
        // одну роль. Теперь рецепт один — cardClass из @/shared/ui/card-style.
        //
        // Без классов символов: `[` ломает разбор селектора, а [ значит
        // ЛИТЕРАЛЬНУЮ скобку. «Что угодно между» — только (?:.|\s)*.
        {
          selector:
            "JSXAttribute[name.name='className'] :matches(Literal[value=/rounded-(?:.|\\s)*\\sborder(?:.|\\s)*\\sp-\\d|(?:^|\\s)p-\\d(?:.|\\s)*\\srounded-(?:.|\\s)*\\sborder/], TemplateElement[value.cooked=/rounded-(?:.|\\s)*\\sborder(?:.|\\s)*\\sp-\\d|(?:^|\\s)p-\\d(?:.|\\s)*\\srounded-(?:.|\\s)*\\sborder/])",
          message:
            'Карточка (рамка + фон + внутренний отступ) рисуется ТОЛЬКО через cardClass из @/shared/ui/card-style: tone задаёт смысл блока, pad — ступень отступа. Нужен новый тон или ступень — добавь в примитив, а не рядом с ним. Роль не карточка (обводка-группировка, рамка картинки, сегментированный контрол) — точечный disable с причиной.',
        }

// Узда предела по вьюпорту (замер ui-drift 27.08.2026). Рецепт «панель не шире и не
// выше экрана» был написан ДЕСЯТЬ раз и ни разу одинаково: поле у краёв выходило то 8,
// то 12, то 16 пикселей, предел высоты — 60, 65 или 70 процентов. Хуже разброса тут
// единица измерения: `vh` на телефоне считает экран ВМЕСТЕ со сворачивающейся панелью
// браузера, поэтому панель на 65vh выше видимой области, и её низ вместе с кнопками
// уезжает под адресную строку. Утилиты cap-viewport и cap-screen закрывают оба случая.
// ⚠️ Без КЛАССОВ СИМВОЛОВ в регулярке: `[` рвёт разбор селектора esquery, и правило
// молча перестаёт что-либо находить. Ровно этот капкан описан выше у правила карточки
// и ровно в него я и попал с первой попытки — узда «сработала» дважды на старом коде и
// не сработала ни разу на подсадном. Литеральная скобка пишется `\u005B`, «что угодно
// между» — `(?:.|\s)*?`. Проверено мутацией: подсадной файл краснеет.
const VIEWPORT_RULE = {
  selector:
    "JSXAttribute[name.name='className'] :matches(Literal[value=/(?:max-)?(?:w|h)-\\u005B(?:.|\\s)*?(?:100vw|\\d+vh|\\d+dvh|\\d+vw)/], TemplateElement[value.cooked=/(?:max-)?(?:w|h)-\\u005B(?:.|\\s)*?(?:100vw|\\d+vh|\\d+dvh|\\d+vw)/])",
  message:
    'Предел по экрану ставят утилиты темы: cap-viewport (ширина — не шире экрана, поле у краёв одно на всё приложение) и cap-screen (высота — 70dvh, dvh а не vh: на телефоне vh считает экран вместе со сворачивающейся панелью браузера). Свой предел по вьюпорту — точечный disable с причиной.',
}

// Узда центрирования: `block` поверх кнопки убивает её центровку.
//
// `buttonClass` строит кнопку на `inline-flex` с `items-center justify-center`. Класс
// `block` меняет display — и обе центровки перестают работать, потому что это свойства
// флекса. Подпись прижимается к верхнему краю при верной высоте, и кнопка выглядит
// сломанной. Владелец называл это ТРИЖДЫ («More выше середины», «Set up MCP access
// прижат к верху»); прошлые заходы правили перенос строки, а не display, — то есть
// чинили похожий симптом.
//
// Нужна полная ширина — `w-full`: он не трогает display и центровку сохраняет.
// ⚠️ Сужено до КНОПОК. Первая редакция ловила любой `block` в любом className и дала 62
// срабатывания, почти все законные: `block` — обычный класс для абзаца, подписи, ссылки в
// тексте. Ровно та ошибка замера, которую этот проект ловил уже восемь раз: правило числит
// законное нарушением, и тогда его отключают целиком. Смотрим ТУДА, где центровка есть:
// вызов `buttonClass` и разметка примитивов кнопки.
const BLOCK_BUTTON_RULE = {
  selector:
    "CallExpression[callee.name='buttonClass'] :matches(Literal[value=/(?:^|\\s)block(?:\\s|$)/], TemplateElement[value.cooked=/(?:^|\\s)block(?:\\s|$)/]), JSXElement[openingElement.name.name=/^(Button|IconButton|SubmitButton)$/] JSXAttribute[name.name='className'] :matches(Literal[value=/(?:^|\\s)block(?:\\s|$)/], TemplateElement[value.cooked=/(?:^|\\s)block(?:\\s|$)/])",
  message:
    'Класс `block` на кнопке убивает её центровку: buttonClass строит кнопку на inline-flex, а items-center/justify-center на блочном элементе не работают — подпись уезжает к верхнему краю. Нужна полная ширина — используй w-full. На НЕ-кнопке (абзац, ссылка в тексте) — точечный disable с причиной.',
}

// Узда ожидания (свип 26.08.2026): роль «идёт работа» была самой массовой из
// переоткрытых руками — 60 мест в 44 файлах и восемь размеров кружка (12, 13, 14,
// 15, 16, 17, 18 и один честный iconSizeFor). Примитива под неё не было вовсе,
// поэтому каждый писал её заново — всегда одинаково и всегда по-своему.
const SPINNER_RULE = {
  selector:
    "JSXAttribute[name.name='className'] :matches(Literal[value=/animate-spin/], TemplateElement[value.cooked=/animate-spin/])",
  message:
    'Ожидание рисует Spinner из @/shared/ui/Spinner: размер приходит от контрола рядом (size), а не подбирается на глаз, и читалка узнаёт про работу там, где кружок стоит один (label). Своя анимация вращения — точечный disable с причиной.',
}

const a11yRecommended = jsxA11y.flatConfigs.recommended.rules

export default [
  { ignores: ['.next/**', 'node_modules/**', 'src/shared/gen/**', 'drizzle/**', 'public/**', '.claude/**'] },
  ...next,
  {
    rules: {
      // preview react-compiler набор (Next 16) — агрессивен на валидных паттернах; держим как warn.
      'react-hooks/immutability': 'warn',
      'react-hooks/set-state-in-effect': 'warn',
      'react-hooks/refs': 'warn',
      'react-hooks/static-components': 'warn',
      'react-hooks/purity': 'warn',
      // Барьер видимости: сырые getListMeta/getTemplateDetail отдают данные БЕЗ canViewList.
      // Забытый гейт = утечка приватного/черновика/снятого модерацией (так родились дыры
      // blame/versions/insights/suggest). Чтение списка идёт ТОЛЬКО через чокпоинт guard.ts.
      // guard.ts импортит из relative './queries' и правилом не задевается (это его законный дом).
      'no-restricted-imports': [
        'error',
        {
          paths: [
            {
              name: '@/features/library/queries',
              importNames: ['getListMeta', 'getTemplateDetail'],
              message:
                'Сырой загрузчик не гейтит видимость. Используй requireViewableMeta/requireViewableDetail из @/features/library/guard. Иная модель доступа (анонимный isPubliclyVisible, owner-only, git/MCP-скоуп) — оставь // eslint-disable-next-line no-restricted-imports с причиной.',
            },
          ],
        },
      ],
      // i18n-барьер: инлайновый двуязычный тернарник (кириллица в ветке) = строка мимо
      // словаря. 696 таких в 129 файлах заморожены в baseline (eslint-suppressions.json);
      // НОВЫЕ — ошибка. Чинишь старые → `npx eslint . --prune-suppressions`. Ложняков нет:
      // кириллица в литерале тернарника — это почти всегда UI-текст.
      'no-restricted-syntax': ['error', ...RESTRICTED],
      // Узда Ф7: <button> без явного type в форме сабмитит её случайно (дефолт
      // submit). Отдельным rule id (не no-restricted-syntax) — чтобы счётные
      // суппрессии i18n-baseline не маскировали кнопочные грехи (Codex #622).
      'react/button-has-type': ['error', { button: true, submit: true, reset: true }],
    },
  },
  // ── Архитектурные границы слоёв (аудит 2026-07-08) ─────────────────────────
  // Направление зависимостей: app → widgets → features → { core, shared }.
  // Фичи не видят друг друга; core не видит никого; shared не видит фичи.
  // Существующие нарушения заморожены в eslint-suppressions.json (baseline):
  // НОВЫЕ кросс-импорты — ошибка сразу; починил старое — запусти
  // `npx eslint . --prune-suppressions`, чтобы усушить baseline.
  {
    files: ['src/**/*.{ts,tsx}'],
    plugins: { boundaries },
    settings: {
      'boundaries/include': ['src/**/*'],
      'boundaries/elements': [
        /**
         * ГНОМЫ — отдельный тип ЧТОБЫ ПОСЧИТАТЬ, а не чтобы запретить.
         *
         * Спор «выносить ли гномов из продукта» (11-razdelenie-gnomov) до сих пор шёл
         * словами: одни говорят «это ядро продукта», другие «это отдельный сервис,
         * который тянет за собой половину кода». Обе стороны правы ровно настолько,
         * насколько велика связность, — а её никто не мерил.
         *
         * Отсюда цель этой границы: ЧИСЛО нарушений в eslint-suppressions.json. Оно и
         * есть ответ. Мало — вынести дёшево; много — вынос означает переписать
         * половину библиотеки, и спор закрыт арифметикой, а не убеждением.
         *
         * ⚠️ СУЩЕСТВУЮЩИЕ НАРУШЕНИЯ НЕ ЧИНИМ. Они заморожены в baseline и служат
         * замером; чинить их сейчас — значит стирать данные, ради которых граница и
         * заведена. Запрещены только НОВЫЕ: связность не должна расти, пока решение
         * не принято.
         *
         * ⚠️ СТОИТ ПЕРЕД `core`/`shared`, И ЭТО НЕ КОСМЕТИКА. Первый совпавший дескриптор
         * выигрывает: пока `gnomes` стоял ниже, `src/shared/ai/council` определялся как
         * `shared`, и половина границы была МЕРТВА — ровно та ловушка «выглядит
         * работающей, не видя ничего», о которой предупреждает абзац выше. Поймано
         * пробным файлом, а не чтением конфига.
         *
         * Состав по признаку «про гномов и совет», а не по каталогу: `shared/ai/gnome-*`
         * и `shared/ai/council*` (личности, репутация, счета, созыв совета),
         * `features/dig` (разговор с гномом) и `features/library/gnome-*` (их задачи и
         * ревью). Общий ИИ-слой (`shared/ai/*` без этих имён) сюда НЕ входит: он нужен
         * продукту и без гномов — генерация, эмбеддинги, модерация.
         */
        // ⚠️ ТОЛЬКО КАТАЛОГИ. Плагин сопоставляет образцы с ПАПКАМИ и на файловый
        // образец отвечает предупреждением («Element patterns match folders, not
        // individual files»): `gnome-account.ts` в тип не попадает, и граница выглядит
        // работающей, не видя ничего. Поэтому здесь стоят только настоящие каталоги, а
        // полный замер связности ведёт отдельный счётчик — scripts/gnomes-coupling.mjs.
        //
        // ⚠️ И ВТОРОЕ, ПОЧЕМУ ГРАНИЦЫ ЗДЕСЬ МАЛО ДЛЯ ЗАМЕРА: подавления в baseline
        // считаются по ПРАВИЛУ на файл, а не по виду нарушения. Файл, у которого уже
        // подавлен кросс-импорт фич, поглотит и новое «features → gnomes» — число
        // связей из него узнать нельзя. Счётчик считает импорты сам и baseline не
        // спрашивает.
        { type: 'gnomes', pattern: 'src/shared/ai/council' },
        { type: 'gnomes', pattern: 'src/features/dig' },
        { type: 'core', pattern: 'src/core' },
        { type: 'shared', pattern: 'src/shared' },
        // mcp и admin — delivery-поверхности (внешний API и админ-консоль над
        // всем продуктом, зеркала app/), а не фичи: им, как и app, можно
        // оркестрировать фичи. Идут ДО features/* — первый матч выигрывает.
        { type: 'mcp', pattern: 'src/features/mcp' },
        { type: 'admin', pattern: 'src/features/admin' },
        // Садовник — АВТОНОМНАЯ ПЕТЛЯ над библиотекой: своей предметной области у
        // него нет, есть только работа над чужой (пишет версии через list-store,
        // извещает через notifications, читает подписки через watch). Это тот же
        // слой оркестрации, что mcp и admin, только запускает его расписание, а не
        // запрос: у Gitea фоновые задачи живут в `services/cron` — рядом с тем, что
        // обслуживает HTTP, а не в слое домена; у Feature-Sliced Design оркестрация
        // нескольких фич принадлежит app (бывший слой processes), а не features.
        //
        // ПРИЗНАК, по которому сюда попадают (проверяемый, а не на вкус): каталог
        // никто не импортирует СНИЗУ — только реестр петель и instrumentation.
        // По этому же признаку кандидаты на следующий заход: linkcheck, digest,
        // backoffice, knowledge (замер 11.08: ноль импортёров у каждого).
        { type: 'gardener', pattern: 'src/features/gardener' },
        { type: 'features', pattern: 'src/features/*', capture: ['feature'] },
        { type: 'widgets', pattern: 'src/widgets' },
        { type: 'app', pattern: 'src/app' },
      ],
      'import/resolver': { typescript: {} },
    },
    rules: {
      'boundaries/dependencies': [
        'error',
        {
          default: 'disallow',
          message:
            'Слой «{{ from.type }}» не должен импортировать «{{ to.type }}» (границы: app → widgets → features → core/shared)',
          policies: [
            // Внутри своего элемента (та же фича, свой слой) — можно всегда.
            { allow: { dependency: { relationship: { to: 'internal' } } } },
            { from: { type: 'shared' }, allow: { to: [{ type: 'core' }] } },
            // Фича видит только себя (internal выше) + core/shared — кросс-импорт фич запрещён.
            { from: { type: 'features' }, allow: { to: [{ type: 'shared' }, { type: 'core' }] } },
            { from: { type: 'widgets' }, allow: { to: [{ type: 'features' }, { type: 'gnomes' }, { type: 'shared' }, { type: 'core' }] } },
            { from: { type: 'mcp' }, allow: { to: [{ type: 'features' }, { type: 'gnomes' }, { type: 'shared' }, { type: 'core' }] } },
            { from: { type: 'admin' }, allow: { to: [{ type: 'features' }, { type: 'gnomes' }, { type: 'shared' }, { type: 'core' }] } },
            { from: { type: 'gardener' }, allow: { to: [{ type: 'features' }, { type: 'gnomes' }, { type: 'shared' }, { type: 'core' }] } },
            // Гномам наружу можно то же, что фиче: они и есть предметная область.
            // Считаем обратное направление — кто тянет ИЗ них, — а его считает
            // `default: disallow` для всех, кто не объявил `gnomes` разрешённым.
            { from: { type: 'gnomes' }, allow: { to: [{ type: 'shared' }, { type: 'core' }, { type: 'gnomes' }] } },
            {
              from: { type: 'app' },
              allow: { to: [{ type: 'mcp' }, { type: 'admin' }, { type: 'gardener' }, { type: 'gnomes' }, { type: 'widgets' }, { type: 'features' }, { type: 'shared' }, { type: 'core' }] },
            },
          ],
        },
      ],
    },
  },
  // ── Доступность: стандартный набор jsx-a11y ───────────────────────────────
  // До 26.08.2026 в проекте работал только тот огрызок правил, который включает
  // eslint-config-next (alt-text и aria-*). Всё остальное — подпись без связи с полем,
  // клик на неинтерактивном элементе, пустая ссылка — не проверялось ничем, при том
  // что линза 07 (интерфейс) один раз это уже вычищала руками: без узды оно вернулось.
  //
  // Плагин уже стоит (транзитивно из next), поэтому регистрировать его нельзя — только
  // включить правила.
  //
  // `label-has-for` выключен намеренно: правило ПОМЕЧЕНО УСТАРЕВШИМ самими авторами и
  // требует htmlFor даже там, где подпись оборачивает контрол — а именно так работает
  // наш `Field` (клик по подписи фокусирует поле без возни с id). Его смысл целиком
  // покрывает `label-has-associated-control`, который включён.
  {
    files: ['src/**/*.tsx'],
    rules: {
      ...Object.fromEntries(Object.keys(a11yRecommended).map((k) => [k, 'error'])),
      'jsx-a11y/label-has-for': 'off',
      // `onError`/`onLoad` на картинке — НЕ взаимодействие человека, а событие загрузки:
      // у Avatar и SmartImage это запасной вид при битой ссылке. По умолчанию правила
      // считают их наравне с кликом и требуют роль — сужаем до мыши и клавиатуры.
      'jsx-a11y/no-noninteractive-element-interactions': ['error', { handlers: ['onClick', 'onMouseDown', 'onMouseUp', 'onKeyPress', 'onKeyDown', 'onKeyUp'] }],
      'jsx-a11y/no-static-element-interactions': ['error', { handlers: ['onClick', 'onMouseDown', 'onMouseUp', 'onKeyPress', 'onKeyDown', 'onKeyUp'] }],
      // Наши контролы — компоненты, а не голые input/select: без этого списка правило
      // не видит контрол внутри подписи и ругается на ЖИВУЮ связку.
      'jsx-a11y/label-has-associated-control': [
        'error',
        {
          controlComponents: ['Input', 'Textarea', 'Select', 'SelectTrigger', 'Checkbox', 'Switch', 'SearchField', 'TagInput', 'FloatingInput', 'ColorSwatch', 'Chip', 'DatePicker'],
        },
      ],
      // Контрол без подписи — ровно то, из-за чего кнопка со значком молчит у диктора.
      // В обоих готовых наборах плагина правило выключено (считается шумным), поэтому
      // включаем сами — но С ЕГО ЖЕ настройками: без них оно требует подпись и у строк
      // таблицы, и у полей ввода, где её даёт label (44 срабатывания против 2).
      'jsx-a11y/control-has-associated-label': [
        'error',
        {
          ...a11yRecommended['jsx-a11y/control-has-associated-label'][1],
          // Ячейка таблицы — не контрол: подписи требует заголовок столбца, а не каждая
          // клетка. В родном списке исключений есть `tr`, а `td`/`th` забыты, и правило
          // требовало подпись у клеток скелетона загрузки.
          ignoreElements: [...a11yRecommended['jsx-a11y/control-has-associated-label'][1].ignoreElements, 'td', 'th'],
        },
      ],
      // ⚠️ `no-autofocus` оставлен ошибкой, но существующие места ЗАМОРОЖЕНЫ в
      // eslint-suppressions.json. Причина: правило не отличает автофокус в окне, открытом
      // человеком (там фокус и обязан быть внутри — WAI-ARIA APG), от автофокуса при
      // загрузке страницы (там он уводит диктора с начала страницы). Все нынешние —
      // первого рода либо страницы одного действия (код 2FA, генерация); НОВЫЙ автофокус
      // потребует решения, а не привычки.
    },
  },
  // ── Узда карточки — ВНЕ shared/ui ─────────────────────────────────────────
  // Внутри shared/ui рамка+фон+отступ — это и есть работа примитива (SettingsSection,
  // PageAside, dropdown-menu, DataTableV2, BubbleToolbar…), запрещать там нечего.
  // Поэтому правило вынесено отдельным блоком с ignores, а не добавлено в общий
  // массив: иначе каждый примитив пришлось бы глушить точечным disable.
  {
    files: ['src/**/*.tsx'],
    ignores: ['src/shared/ui/**'],
    rules: {
      'no-restricted-syntax': ['error', ...RESTRICTED, CARD_RULE, SPINNER_RULE, VIEWPORT_RULE, BLOCK_BUTTON_RULE],
    },
  },
]
