// Точка входа прод-миграции. Отдельным файлом, чтобы scripts/migrate-push.ts остался
// модулем: из него импортируются проверяемые куски (clearDanglingForks), и такой
// импорт не должен применять схему. Здесь же main зовётся безусловно — не бывает
// случая, когда этот файл запустили, а миграция не пошла.
import { main } from './migrate-push'

main().catch((e) => {
  console.error('[migrate] ошибка:', e)
  process.exit(1)
})
