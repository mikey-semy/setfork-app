# Отпечатки находок, признанных НЕ секретами. Формат: коммит:файл:правило:строка —
# то есть запись адресная: она гасит ровно одно место в одном коммите, а не правило
# целиком. Правило гасить нельзя: generic-api-key — главный ловец в этом репозитории.
#
# Как получить отпечаток: гейт секретов печатает правило, файл, строку и коммит
# (.github/workflows/audit.yml). Полный отпечаток — в JSON-отчёте того же прогона.

# Пароль одноразовой персоны в скрипте живого замера: строка вида «Xxx-xxx-123» по
# форме неотличима от ключа, и правило сработало верно. Значение секретом никогда не
# было — персона создаётся замером на локальном стенде и живёт один прогон. Сам
# литерал убран (пароль теперь сочиняется на запуске), но в ЭТОМ коммите он остался,
# а история не переписывается ради тестовой строки: переписывание оправдано для
# настоящего секрета, который надо ещё и отозвать, — здесь отзывать нечего.
f69a8756bab5c39b66e8cd5c5319b43d95230f3f:scripts/probe/touch-sweep.mjs:generic-api-key:31

# Тестовый ключ IndexNow в юнит-тесте файла ключа: строка «a1b2c3d4-e5f6-7890» по форме
# неотличима от ключа, правило сработало верно. Секретом она не была никогда — ключ
# IndexNow публичен по самому протоколу (лежит в /<ключ>.txt у всех на виду), а эта
# строка нигде не задана. Литерал заменён заглушкой в следующем коммите, здесь остался.
9ff65c2cacddfd08f86bdc83c97dcac5ecb7a847:tests/app/indexnow-key-file.test.ts:generic-api-key:16
