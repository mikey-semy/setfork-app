node_modules/
.next/
.env
.env.local
.env.production
.DS_Store
*.log
.idea/
.vscode/
next-env.d.ts
dist/
build/

# TS build info
*.tsbuildinfo

# Uploaded user content (avatars) — not committed
/public/uploads/

# SetFork git-as-source-of-truth persistent repos
.setfork-git/
coverage/

# Local eval/benchmark output (generated, timestamped — ablation-eval, #478)
/scripts/ablation-eval-result.json

# Артефакты прогонов Playwright (смок гоняется из setfork-sim, сюда попадают случайно)
test-results/
playwright-report/
itest-run.log
