/**
 * Экшены предложений — вход прежний.
 *
 * Файл БЕЗ 'use server' намеренно, и это не вкусовщина: в модуле с этой директивой
 * `export { … } from` НЕ РАБОТАЕТ. Next оставляет наружу только объявленные в самом
 * файле асинхронные функции, а реэкспорт вырезает — сборка падает с «The module has
 * no exports at all», и `tsc` этого не видит (проверено на fe#758). Барель без
 * директивы прозрачен: так же устроен соседний `index.ts`.
 *
 * Нужен затем, чтобы разбор не заставлял страницу обсуждения переписывать импорт.
 */
export * from './suggestion-submit'
export * from './suggestion-branch'
export * from './suggestion-items'
export * from './suggestion-comments'
