import { MAX_LISTS_PER_USER } from '@/shared/quota'

/**
 * Предел одной пачки.
 *
 * Число не выбрано на глаз: больше собственной квоты списков человеку и не принадлежит
 * (`MAX_LISTS_PER_USER`, она же настраивается env'ом), значит и трогать за раз больше
 * нечего. Владельцу с корпусом сверх квоты пачка просто идёт частями.
 *
 * Живёт отдельным файлом, а не рядом с действиями: модуль с 'use server' обязан
 * экспортировать только асинхронные функции, и константе там места нет.
 */
export const BULK_MAX = MAX_LISTS_PER_USER
