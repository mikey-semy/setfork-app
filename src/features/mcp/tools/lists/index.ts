/**
 * Запись списков через MCP: создание, правка, удаление, публикация накопленного.
 *
 * Вход прежний — `./lists` из `tools/index.ts`, маршрут и реестр инструментов
 * разбора не видят. Внутри разложено по причинам измениться:
 *
 * | модуль | меняется, когда |
 * |---|---|
 * | `create` | меняются правила появления списка (квота, дедуп, язык) |
 * | `delete` | меняются условия необратимого удаления |
 * | `edit` | меняется сам сюжет правки: обновить, патчить, опубликовать, выбросить |
 * | `write` | меняется путь записи состава (владение, черновик против версии) |
 * | `draft-store` | меняется схема шагов и прогонов |
 * | `patch-block` | появился тип блока или поле в плоской форме MCP |
 */
export * from './create'
export * from './delete'
export * from './edit'
