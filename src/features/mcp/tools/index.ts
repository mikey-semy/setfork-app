/**
 * Инструменты MCP: то, что агент вызывает по токену вместо браузера.
 *
 * Вход прежний (`@/features/mcp/tools`), внутри разложено по сюжетам: чтения, запись
 * списков, предложения правок, источники знаний, прогоны. Фасад держит маршрут
 * `api/[transport]` в неведении о разборе — он импортирует двадцать два инструмента
 * одной строкой, и трогать его незачем.
 */
export * from './lists'
export * from './shared'
export * from './runs'
export * from './suggestions'
export * from './sources'
export * from './reads'
