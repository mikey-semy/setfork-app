/**
 * Инструменты MCP: то, что агент вызывает по токену вместо браузера.
 *
 * Вход прежний (`@/features/mcp/tools`), внутри разложено по сюжетам: чтения, запись
 * списков, предложения правок, источники знаний, прогоны. Фасад держит маршрут
 * `api/[transport]` в неведении о разборе — он импортирует двадцать два инструмента
 * одной строкой, и трогать его незачем.
 *
 * Все инструменты работают от имени пользователя токена (userId). Приватность
 * соблюдается: getFeed/visibleFilter уже фильтруют по viewerId, get_list проверяет
 * доступ явно. Контент отдаём в EN (locale-JSON, tr с фолбэком).
 */
export * from './lists'
// Из общего наружу торчат только ТИПЫ входных блоков — их знает patch.ts и тесты.
// Сами помощники (проверка видимости, конвертеры) остаются деталью каталога:
// `export *` сделал бы их публичным API модуля, хотя они нужны только соседям.
export type { McpBlockOption, McpItemInput } from './shared'
export * from './runs'
export * from './suggestions'
export * from './sources'
export * from './reads'
