/**
 * Потолок вариантов в одной генерации.
 *
 * Считаются ДОСТАВЛЕННЫЕ варианты, а не попытки (решение владельца 2026-08-01): раньше потолок
 * упирался в номер витка, поэтому шесть сорвавшихся прогонов запирали человека на экране с нулём
 * вариантов и сообщением «достигнут предел в 6 вариантов» — счёт за неудачи ядра выставлялся ему.
 * От перерасхода защищают свои механизмы: rate-limit на генерацию и месячная квота черновиков.
 *
 * Число живёт здесь, а не пятью литералами в действиях и двумя в UI: сервер и экран обязаны
 * говорить об одном и том же пределе (раньше UI считал варианты, сервер — витки, и кнопка была
 * активной ровно до отказа сервера).
 */
export const MAX_VARIANTS = 6
