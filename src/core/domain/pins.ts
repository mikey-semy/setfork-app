/**
 * ЗАКРЕПЛЁННЫЕ СПИСКИ — одно правило на оба входа: окно «Настроить» на профиле и
 * кнопку «Закрепить» в шапке списка.
 *
 * Форма взята у GitHub (Edit pinned items): до шести, и только то, что увидят все.
 * Раньше правило жило частями: окно предлагало и приватные списки, и черновики —
 * такой занимал слот, а посетитель его не видел, — а кнопка в шапке не знала предела
 * вовсе и закрепляла седьмой, восьмой и дальше.
 */
export const MAX_PINS = 6
