/**
 * Имя куки, в которой лежит выбор «сайдбар свёрнут/развёрнут».
 *
 * Отдельным модулем, потому что её просят ОБЕ стороны: сервер читает её в layout, чтобы
 * нарисовать сайдбар сразу верным, клиент пишет при переключении. Из `use client`-модуля
 * серверу можно брать только компоненты — правило под архитектурным тестом
 * (`tests/architecture/rsc-client-boundary`), и общая константа обязана жить вне его.
 */
export const SIDEBAR_COOKIE = 'sf_sidebar'
