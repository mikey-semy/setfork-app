/**
 * МАСКА СЕКРЕТА — ОДНА НА ПРОЕКТ, И ОНА НИЧЕГО НЕ РАСКРЫВАЕТ.
 *
 * Копий было три, и все три звались «для показа секрета в UI без раскрытия», раскрывая:
 *   • `settings/media.ts` — первые 4 и последние 4 знака;
 *   • `settings/email.ts` — первые 2 и последние 2;
 *   • `settings/ai.ts` (`maskKey`) — первые ВОСЕМЬ и последние 4.
 * Комментарий врал у каждой: обещание «без раскрытия» стояло над кодом, который
 * раскрывает. Разошлись они и по степени, и по имени — к паролю почты применялась
 * САМАЯ болтливая из трёх (страница админки импортировала `maskSecret` из `media`,
 * а не из `email`), и снимок формы отдавал `Svxk••••••••442!`.
 *
 * Почему края и длина — это раскрытие, а не украшение:
 *   • длина сама по себе сужает перебор;
 *   • у ключей поставщиков первые знаки — это ПРЕФИКС (`sk-or-v1-…`), то есть
 *     раскрытые 8 знаков сообщают вид ключа и часть значения;
 *   • снимок экрана уходит в переписку, в задачу, в поддержку — и уносит половину.
 *
 * Поле в форме отвечает на вопрос «задан или нет», а не «какой именно». Поэтому маска
 * фиксированной длины: по ней нельзя ни угадать секрет, ни оценить его длину.
 */
export const SECRET_MASK = '••••••••'

/** Маска для показа: пусто → пусто (поле не заполнено), иначе фиксированные точки. */
export function maskSecret(v: string | null | undefined): string {
  return v ? SECRET_MASK : ''
}
