/**
 * СКОЛЬКО СПИСКОВ ПОПАДАЕТ В МАШИННЫЕ ФАЙЛЫ — оба числа рядом.
 *
 * `llms.txt` перечисляет ссылки, `llms-full.txt` разворачивает содержимое. Числа разные
 * и по делу: перечислить дёшево, развернуть — дорого (каждый список это несколько
 * запросов и полный markdown).
 *
 * ⚠️ ЖИВУТ ВМЕСТЕ, потому что `llms.txt` о втором файле РАССКАЗЫВАЕТ. Пока числа стояли
 * в разных маршрутах, текст утверждал «те же списки» при 50 против 20 — неправда для
 * тридцати записей, которую человеку неоткуда заметить: файл читают агенты.
 */
/** Сколько списков ПЕРЕЧИСЛЯТЬ: файл — указатель, а не выгрузка корпуса. */
export const LLMS_LISTED = 50
/**
 * Сколько списков ВКЛАДЫВАТЬ ЦЕЛИКОМ. Файл читают модели с ограниченным окном, и
 * бесконечная выгрузка корпуса делает его бесполезным для всех: указатель уже есть в
 * `llms.txt`, а за остальным — ссылки.
 */
export const LLMS_INLINED = 20
