/**
 * Путь текущего запроса (вместе с query) — серверным компонентам.
 *
 * В App Router компонент своего пути не знает, поэтому его кладёт middleware. Имя
 * заголовка живёт здесь, а не строкой в двух местах: разъехавшись, они не сломали бы
 * ни сборку, ни тесты — просто перенаправление тихо потеряло бы хвост пути.
 *
 * Заголовок приходит ОТ middleware, а не от клиента: `NextResponse.next({request})`
 * подменяет заголовки запроса на входе, и внешнее значение до компонента не доходит.
 */
export const REQUEST_PATH_HEADER = 'x-request-path'
