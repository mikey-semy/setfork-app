/**
 * ДЛИНА ПАРОЛЯ — ОДНИМ ЧИСЛОМ НА ВСЕ ТРИ ФОРМЫ.
 *
 * Регистрация, сброс по письму и смена в настройках проверяли длину каждая своим
 * литералом. Разойдись они — форма примет пароль, который сервер отвергнет, и человек
 * увидит отказ без причины.
 *
 * Модуль НЕ `server-only`: то же число нужно браузеру в `minLength`, чтобы отказ
 * приходил до отправки.
 */
export const MIN_PASSWORD_LENGTH = 8
