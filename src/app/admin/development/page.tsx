import { redirect } from 'next/navigation'

/**
 * «Развитие» переехало в раздел «Компания» (2026-07-31): компания — не настройка инстанса,
 * ей место в своём разделе, а не вперемешку с админскими.
 *
 * Редирект оставлен, а не удалён: на этот адрес ведут закладки владельца и письма бухгалтера
 * с летописцем, отправленные ДО переезда, — они живут в почте и после выката.
 */
export default async function DevelopmentMovedPage() {
  redirect('/admin/company')
}
