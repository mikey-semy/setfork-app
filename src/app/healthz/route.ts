/**
 * `/healthz` — тот же ответ, что у `/api/health`, по общепринятому адресу.
 *
 * Проба живости у нас лежит на `/api/health`, а внешние наблюдатели (облачные
 * балансировщики, uptime-мониторы, k8s-подобные оркестраторы) по умолчанию спрашивают
 * `/healthz`: это соглашение, а не наша выдумка. Наблюдатель, настроенный по умолчанию,
 * получал 404 и считал приложение мёртвым — то есть проба показывала обратное тому, что
 * есть (находка R4).
 *
 * ⚠️ ОДНА РЕАЛИЗАЦИЯ, ДВА АДРЕСА. Обработчик берётся из `/api/health`, а не пишется
 * заново: две пробы живости неизбежно разошлись бы — одна научилась бы чему-то
 * (таймаут, проверка воркеров), вторая осталась бы прежней, и какая из них правда,
 * выяснялось бы в аварии.
 *
 * ⚠️ БЕЗ БАЗЫ, как и оригинал: живость — про процесс, а не про зависимости. «Готов ли
 * обслуживать» отвечает `/api/ready`, и путать их нельзя: при мёртвой базе живой
 * процесс обязан отвечать 200 на liveness, иначе оркестратор перезапустит его по кругу,
 * не починив причину.
 */
/**
 * ⚠️ `runtime`/`dynamic` объявлены ЗДЕСЬ ЛИТЕРАЛАМИ, а не реэкспортом. Next разбирает
 * их статически при сборке, и `export { runtime } from '…'` он не принимает —
 * реэкспорт роняет НЕ ЭТОТ РОУТ, А ВЕСЬ СЕРВЕР: 500 на каждом запросе, включая
 * `/api/health`, который сам по себе исправен. Поймано живой проверкой кодом ответа;
 * ни типы, ни тесты этого не видят, потому что тест импортирует модуль напрямую, минуя
 * сборку сегментов.
 *
 * Реэкспорт самого обработчика при этом законен и нужен: одна реализация на два адреса.
 */
export const runtime = 'nodejs'
export const dynamic = 'force-dynamic'

export { GET } from '../api/health/route'
